# 图像缩小、恢复与频域分析实验
## 📋 项目概述
本项目基于OpenCV实现灰度图像的下采样、插值恢复与频域分析，完整覆盖数字图像处理课程上机实验要求，包含空间域质量评价、傅里叶变换(FFT)、离散余弦变换(DCT)分析，可直接生成实验报告所需的可视化结果与量化指标。

---
## 环境依赖
```bash
# 安装核心依赖
pip install opencv-python numpy matplotlib
```
- 系统：Ubuntu 22.04 (WSL) / Windows
- 语言：Python 3.x
- 核心库：OpenCV、NumPy、Matplotlib

---
## 项目结构
```
cv-course/
├── syzy.py                # 完整实验代码
├── test_image.jpg         # 输入测试图像（需自行替换）
├── result_images.png      # 空间域图像对比结果
├── result_fft.png         # FFT频谱分析结果
└── result_dct.png         # DCT系数与能量分析结果
```

---
## 实验原理
### 1. 核心流程
`图像读入 → 两种下采样（直接/高斯滤波） → 三种插值恢复 → 空间域质量评估 → FFT频域分析 → DCT频域分析 → 结果可视化`

### 2. 关键技术
| 模块 | 核心方法 | 原理说明 |
|------|----------|----------|
| 下采样 | 直接下采样 / 高斯滤波+下采样 | 直接采样保留细节但易混叠；高斯滤波抑制混叠，图像更平滑 |
| 插值恢复 | 最近邻 / 双线性 / 双三次 | 最近邻计算快但锯齿明显；双线性平滑均衡；双三次细节保留最优 |
| 空间域评价 | MSE / PSNR | MSE越小、PSNR越大，恢复质量越好 |
| FFT分析 | 二维傅里叶变换+频谱中心化 | 低频对应图像结构，高频对应边缘细节，直观展示细节丢失 |
| DCT分析 | 二维DCT+低频能量占比 | 能量集中在左上角低频，占比越高，图像越平滑 |

---
## 实验结果
### 1. 量化指标输出
```
原始图像尺寸: 3444x2296
下采样后尺寸: 1722x1148

=== 直接下采样恢复结果（vs 原图） ===
最近邻: MSE=34.75, PSNR=32.72 dB
双线性: MSE=21.37, PSNR=34.83 dB
双三次: MSE=21.75, PSNR=34.76 dB

=== 高斯下采样恢复结果（vs 原图） ===
最近邻: MSE=33.89, PSNR=32.83 dB
双线性: MSE=27.79, PSNR=33.69 dB
双三次: MSE=23.84, PSNR=34.36 dB

运行完成！已生成三张结果图片：
1. result_images.png
2. result_fft.png
3. result_dct.png
```

### 2. 可视化结果说明
#### 图1：`result_images.png`（空间域对比图）
- **对应位置**：实验报告「空间域可视化结果分析」章节
- **内容**：3行3列布局，完整展示原图、两种下采样、6种恢复效果
  - 第1行：原图、直接下采样、高斯下采样
  - 第2行：直接下采样的三种插值恢复结果
  - 第3行：高斯下采样的三种插值恢复结果
- **分析要点**：对比不同下采样方式的混叠抑制效果、三种插值的边缘/细节差异

#### 图2：`result_fft.png`（FFT频谱图）
- **对应位置**：实验报告「傅里叶变换结果分析」章节
- **内容**：3张子图，分别为原图频谱、直接下采样频谱、双线性恢复频谱
- **分析要点**：下采样的低通滤波特性（高频丢失）、插值无法还原高频细节、双线性的平滑效应

#### 图3：`result_dct.png`（DCT系数图）
- **对应位置**：实验报告「离散余弦变换结果分析」章节
- **内容**：3张子图，分别为原图DCT、双线性恢复DCT、双三次恢复DCT，标注低频能量占比
- **分析要点**：插值平滑性与低频能量占比正相关，双三次细节保留优于双线性

---
## 快速运行指南
1.  **替换图像路径**：修改代码中`img_path`为你的测试图像绝对路径（如`/home/chen/cv-course/test_image.jpg`）
2.  **运行代码**：
    ```bash
    cd /home/chen/cv-course
    python syzy.py
    ```
3.  **获取结果**：运行完成后，在项目目录下自动生成3张结果图，无弹窗警告（已适配Linux无图形界面环境）

---
## 实验结论
1.  **下采样优化**：高斯预滤波可有效抑制混叠失真，提升视觉质量，但会轻微降低PSNR
2.  **插值性能排序**：双线性 ≈ 双三次 > 最近邻，双线性综合性价比最高
3.  **不可逆性验证**：下采样会不可逆丢失高频细节，插值仅能近似恢复低频结构
4.  **频域量化**：DCT低频能量占比可量化图像平滑度，为算法选择提供依据

---
## 实验报告插入说明
| 图片 | 报告插入位置 | 分析要点 |
|------|--------------|----------|
| `result_images.png` | 空间域结果分析章节 | 直观展示下采样与插值的视觉效果差异 |
| `result_fft.png` | FFT分析章节 | 验证下采样的低通特性与插值的细节还原能力 |
| `result_dct.png` | DCT分析章节 | 量化不同插值的平滑性与细节保留能力 |

---
## 常见问题解决
- **文件找不到**：使用绝对路径替换相对路径，确保图像文件存在
- **Matplotlib无法显示**：代码已默认使用`plt.savefig()`保存图片，无需弹窗
- **混叠失真明显**：增大高斯滤波核大小（如`ksize=(7,7)`）增强平滑效果

---
## 代码说明
完整代码见`syz y.py`，已完成以下优化：
- 适配Linux无图形界面环境，自动保存可视化结果
- 完整覆盖实验所有要求，包含量化计算与频域分析
- 模块化设计，可直接修改下采样比例、滤波参数等

